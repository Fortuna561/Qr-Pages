name: Build
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Execute script
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUBTOKEN }}
        run: node script.js
